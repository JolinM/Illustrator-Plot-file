if ( app.documents.length > 0 ) {   var myDoc = app.activeDocument;//remove black         app.activeDocument.swatches.getByName('Black').remove(); //add Black swatchvar newSwatch = app.activeDocument.swatches.add() var newColor = new GrayColor();newColor.gray = 100;newSwatch.name = "Black" newSwatch.color = newColor//Sélectionner les lignes par spot color//var findColor = swatch.getByName("AutoCAD Color 2").color;//var replaceColor = swatch.getByName("Black").color;//pathItem.strokeColor/*var idoc = app.activeDocument;var itext = idoc.textFrames[0];//alert(itext.textRange.characterAttributes.fillColor);var fillColor = itext.textRange.characterAttributes.fillColor;var strokeColor = itext.textRange.characterAttributes.strokeColor;filled = stroked = "yes";if (fillColor=="[NoColor]")        filled = "No";if (strokeColor=="[NoColor]")        stroked = "No";alert("Filled: " + filled + "\n" + "Stroked: " + stroked);*/// Changer la graisse en plus de la couleur et de la teinte de la couleur/* Changer la couleurthisItem.strokeColor = doc.swatches.getByName("Black").color;*/// doc.layers.getByName('Through Cut').pathItems.strokeColor = doc.swatches.getByName("Black").color;/*//changer la spot color en Blackvar paths = myDoc.pathItems;for (var i=0; i<paths.length; i++) {    var Autocad20 = paths[i].strokeColor;    if (Autocad20 instanceof SpotColor // is Spot        && Autocad20.typename== "AutoCAD Color 20" //check name    ) {        paths[i].strokeColor = newStroke;    }}}*//*// Changer tous lesof a llpath item to Blackif ( app.documents.length > 0 && app.activeDocument.pathItems.length > 0 ) {doc = app.activeDocument;for (var i = 0; i < doc.pathItems.length; i++ ) {pathRef = doc.pathItems[i];pathRef.stroked = true;pathRef.strokeColor = doc.swatches["Black"].color;}}}*/}