if ( app.documents.length > 0 ) {  var replaceColor = swatches.getByName("Black").color; for (var i = 0; i < pathItems.length; i++) {with (pathItems[i]) {if (filled == true && fillColor instanceof SpotColor) {if (fillColor.spot.name == "AutoCAD Color 20") fillColor = replaceColor;}if (stroked == true && strokeColor instanceof SpotColor) {if (strokeColor.spot.name == "AutoCAD Color 20") strokeColor = replaceColor;}}} for (var j = 0; j < stories.length; j++) {with (stories[j]) {for (var k = 0; k < characters.length; k++) {with (characters[k].characterAttributes) {if (fillColor instanceof SpotColor) {if (fillColor.spot.name == "AutoCAD Color 20") fillColor = replaceColor;}if (strokeColor instanceof SpotColor) {if (strokeColor.spot.name == "AutoCAD Color 20") strokeColor = replaceColor;}}}}}}